{
    "buildCommand": "yarn workspace @project/shared build && yarn workspace client build",
    "outputDirectory": "client/dist",
    "installCommand": "yarn install",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}